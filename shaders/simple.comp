#version 450
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set=0, binding=0) buffer ImageStuff {
    float[] pixels;
};

uint WIDTH = 400;

void main() {
    uint i = gl_GlobalInvocationID.x;

    uint x = i % WIDTH;
    uint y = i / WIDTH;

    pixels[i] = float(x) / float(WIDTH);
}
